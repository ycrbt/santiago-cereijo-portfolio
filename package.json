{
  "name": "santiago-cereijo-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-worker.mjs",
    "build:pages": "node build-pages.mjs",
    "check": "node --check script.js && node --check build-worker.mjs && node --check build-pages.mjs"
  }
}
